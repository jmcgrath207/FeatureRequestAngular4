import { ReverseIteratePipe } from './reverse-iterate.pipe';

describe('ReverseIteratePipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseIteratePipe();
    expect(pipe).toBeTruthy();
  });
});
